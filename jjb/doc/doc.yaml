---
- project:
    name: doc
    project-name: 'doc'

    jobs:
      - '{project-name}-{stream}-verify-rtd':
          build-node: 'master'
      - '{project-name}-{stream}-merge-rtd'

    project: 'doc'


    rtdproject: 'onap'

    stream:
      - master:
          branch: 'master'
